<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Workout</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>

    <!-- Navigation Bar -->
    <nav>
        <a href="/">Home</a> |
        <a href="/workouts">View Workouts</a> |
        <a href="/workouts/new">Add Workout</a>
    </nav>

    <div class="container">
        <h1>Edit Workout</h1>

        <form action="/workouts/<%= workout._id %>?_method=PUT" method="POST" class="form-box">
            
            <label for="name">Workout Name:</label>
            <input type="text" id="name" name="name" value="<%= workout.name %>" required>

            <label for="date">Date:</label>
            <input type="date" id="date" name="date" value="<%= workout.date.toISOString().slice(0,10) %>" required>

            <label for="duration">Duration (in minutes):</label>
            <input type="number" id="duration" name="duration" value="<%= workout.duration %>" required>

            <button type="submit" class="btn-save">Save Changes</button>
        </form>

        <a class="btn-back" href="/workouts">⬅ Back to Workouts</a>
    </div>

</body>
</html>
